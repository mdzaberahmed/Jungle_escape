name: jungle_escape
description: A 3D Jungle Survival RPG.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  model_viewer_plus: ^1.7.0 # ৩ডি মডেল ভিউয়ার প্যাকেজ 📦

flutter:
  uses-material-design: true
  assets:
    - assets/models/archer.glb # ৩ডি মডেল ফাইল 🏹
    - assets/images/player_avatar.png # প্লেয়ার অবতার ইমেজ 👤
    
